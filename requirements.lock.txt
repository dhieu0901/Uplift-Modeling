# Exact versions used to produce every number in README.md, on CPython 3.12.4
# (Windows 11, 20 logical cores). Direct dependencies only; transitive versions
# are left to the resolver because no result in this project depends on them.
#
# Regenerate the numbers with:
#   python -m pip install -r requirements.lock.txt
pandas==3.0.2
numpy==2.4.4
scikit-learn==1.7.2
scipy==1.17.1
matplotlib==3.10.8
duckdb==1.5.4
pyarrow==23.0.0
lightgbm==4.6.0
scikit-uplift==0.5.1
joblib==1.5.2
# Pinned so the repo installs complete. streamlit renders numbers that were
# already computed and produces none of its own, so its version cannot move a
# result. The notebook stack is here for the same reason: it runs the analysis
# in src/ rather than restating it.
#
# 1.62.0 is a floor rather than a preference. Earlier releases cap numpy below
# 2 or pandas below 3, either of which contradicts the pins above and makes the
# whole file unresolvable. A machine can end up holding an older streamlit
# beside numpy 2 if the two were installed at different times, but pip will
# never build that state from this file, so the pin has to be one pip can
# actually satisfy.
streamlit==1.62.0
jupyter==1.1.1
ipykernel==6.29.5
nbformat==5.9.2
nbconvert==7.10.0
pytest==8.3.5
ruff==0.14.13
