# Minimum supported versions. Use requirements.lock.txt to reproduce the exact
# environment that produced the numbers in README.md.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
duckdb>=1.1
# pandas needs a parquet engine. Production I/O goes through DuckDB, but the
# test suite writes fixtures with DataFrame.to_parquet, which requires this.
pyarrow>=14.0
lightgbm>=4.0
scikit-uplift>=0.5
# Serving. joblib writes the saved policy; streamlit only renders numbers that
# were already computed, so it takes part in no result.
joblib>=1.3
streamlit>=1.30
pytest>=8.0
ruff>=0.5
